# Railway CLI local link metadata (do not commit; each clone runs `railway link`).
.railway/

node_modules/
dist/
.expo/
.DS_Store
# Local env / secrets (keep *.example templates tracked)
.env
.env.*
!.env.example
!.env.*.example
**/.env
**/.env.*
!**/.env.example
!**/.env.*.example
.crewcue-dev-env
*.log
*.tsbuildinfo
coverage/
.terraform/
*.tfstate
*.tfstate.*

# Local IDE/project artifacts
.idea/

# Ephemeral agent simulator artifacts — attach to PR only, never commit
.agent-pr-evidence/

# Accidental tsc emit next to the pacing fixture helper (import the .ts from tests)
fixtures/pacing/*.js
fixtures/pacing/*.d.ts
fixtures/pacing/*.d.ts.map

# Expo prebuild output when run from repo root
/android/
/ios/
/app.json
