/**
 * Pixels the sheet overlaps the title chrome upward. Keep **0** so the Modal does not
 * paint over the header title (that caused clipping). Kept as a named export for bundles.
 */
export const TOOLTIP_SHEET_SEAM_OVERLAP = 0;

/** @deprecated Same as {@link TOOLTIP_SHEET_SEAM_OVERLAP}; kept for older bundle chunks. */
export const TOOLTIP_LIP_OVERLAP = TOOLTIP_SHEET_SEAM_OVERLAP;
