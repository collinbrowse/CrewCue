JWT_SECRET=replace-with-dev-secret
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318/v1/traces
PORT=4000
# MapTiler Cloud key for server-side geocode proxy (`GET /race-rooms/:roomId/geocode/search`).
# Distinct from mobile/web tile keys (`EXPO_PUBLIC_MAPTILER_*` / `VITE_MAPTILER_*`) when rotating secrets.
MAPTILER_API_KEY=
HOST=0.0.0.0
PERSISTENCE_MODE=postgres
# LOCAL DEV ONLY (docker-compose.local.yml defaults). Never reuse these credentials outside local development.
DATABASE_URL=postgres://crewcue:crewcue@localhost:5432/crewcue
